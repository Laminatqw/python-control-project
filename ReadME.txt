посилання на колекцію з запитами в постмані:https://api.postman.com/collections/37377666-279d0c7f-3a7c-4435-aca8-cde21443eaea?access_key=PMAT-01JE2FKZM6R167NN554BEQJBCD

Щоб запустити бек проекту необхідно використовуючи docker desktop прописати у консолі "docker conpose up --build"

В проекті було реалізовано:

Ролі, також два типа акаунтів(звичайний та преміумний)

Користувач може зареєструватись використовуючи електронну пошту та пароль(/auth)

Користувач може створити оголошення на продаж авто задавши такі параметри як: модель, марку, ціну, регіон, опис (/listing/create)

Користувач з преміумним аккаунтом має можливість переглянути статистику відвідуваности сторінки певного автомобіля(/listing/:id/stats)

Користувач може дізнатись середню ціну автомобіля по області (/listing/average)

Список з усіма брендами, що є у базі даних(/cars/brands)

Решта задач реалізовано не було, також як і загрузки на AWS

в результаті виконання було замічено баг(а може й не баг) - access токен ставав невалідним після першого використання
